// Base URL of the backend API.
// - Testing on a phone with Expo Go: use your LAPTOP'S network IP, e.g. http://192.168.1.50:3000
//   (localhost won't work — on the phone that means the phone itself).
// - Once deployed: change this to the AWS URL.
export const BASE_URL = 'http://localhost:3000'
